# Gemfile contains the list of gems to install
source 'https://rubygems.org' # use public gem repo
gem 'selenium-webdriver' # framework that allows to simulate human interaction with a browser
gem 'webdrivers', '~> 3.0'
gem 'minitest'